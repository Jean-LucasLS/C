#include <stdio.h>
#include <stdlib.h>

typedef struct{
    int value;
    struct Info *ant;
}Info;

typedef struct{
    int size;
    Info *top;
}Jujuba;

void startStack(Jujuba *stackaux){
    int i;
    return;
}

int stackNumber(Jujuba *stackaux, int n){
    Info *p;
    p = (Jujuba *)malloc(sizeof(Info));
}

int main(){
    int L, i, n, nbin, ntransbin, ndec;
    Jujuba stack;
    Info jockey;
    scanf("%d", &L);
    for(i = 0; i < L; i++){
        scanf("%d", n);
    }
    return 0;
}